diff --git a/include/rf2o_laser_odometry/CLaserOdometry2DNode.hpp b/include/rf2o_laser_odometry/CLaserOdometry2DNode.hpp
index 8a1e170..5d3615b 100644
--- a/include/rf2o_laser_odometry/CLaserOdometry2DNode.hpp
+++ b/include/rf2o_laser_odometry/CLaserOdometry2DNode.hpp
@@ -1,5 +1,7 @@
 #include "rf2o_laser_odometry/CLaserOdometry2D.hpp"
 
+#include <array>
+
 #include <tf2/convert.h>
 #include <tf2/exceptions.h>
 #include <tf2_ros/transform_broadcaster.h>
@@ -36,6 +38,9 @@ public:
   std::shared_ptr<tf2_ros::TransformListener>     tf_listener_;  
   std::unique_ptr<tf2_ros::TransformBroadcaster>  odom_broadcaster;
   nav_msgs::msg::Odometry                         initial_robot_pose;
+  std::array<double, 3>                           accumulated_pose_variance;
+  rclcpp::Time                                    previous_odom_time;
+  bool                                            have_previous_odom_time;
 
   // Subscriptions & Publishers
   rclcpp::Subscription<sensor_msgs::msg::LaserScan>::SharedPtr  laser_sub;
@@ -46,4 +51,3 @@ public:
   void LaserCallBack(const sensor_msgs::msg::LaserScan::SharedPtr new_scan);
   void initPoseCallBack(const nav_msgs::msg::Odometry::SharedPtr new_initPose);
 };
-
diff --git a/src/CLaserOdometry2D.cpp b/src/CLaserOdometry2D.cpp
index 210bc04..4894fd8 100644
--- a/src/CLaserOdometry2D.cpp
+++ b/src/CLaserOdometry2D.cpp
@@ -273,7 +273,7 @@ bool CLaserOdometry2D::odometryCalculation(const sensor_msgs::msg::LaserScan& sc
 
   // Get computation time 
   auto m_runtime = get_clock()->now() - start;
-  RCLCPP_INFO(get_logger(), "execution time (ms): %f",
+  RCLCPP_DEBUG(get_logger(), "execution time (ms): %f",
                 m_runtime.seconds()*double(1000));
 
   // Update poses with the new odom
@@ -966,7 +966,7 @@ void CLaserOdometry2D::PoseUpdate()
   kai_loc_old_(1) = -kai_abs_(0)*std::sin(phi) + kai_abs_(1)*std::cos(phi);
   kai_loc_old_(2) =  kai_abs_(2);
 
-  RCLCPP_INFO(get_logger(), "Laser odom [x,y,yaw]=[%f %f %f]",
+  RCLCPP_DEBUG(get_logger(), "Laser odom [x,y,yaw]=[%f %f %f]",
                 laser_pose_.translation()(0),
                 laser_pose_.translation()(1),
                 rf2o::getYaw(laser_pose_.rotation()));
@@ -974,7 +974,7 @@ void CLaserOdometry2D::PoseUpdate()
   // Compose Transformations (robot odom)
   robot_pose_ = laser_pose_ * laser_pose_on_robot_inv_;
 
-  RCLCPP_INFO(get_logger(), "Robot-base odom [x,y,yaw]=[%f %f %f]",
+  RCLCPP_DEBUG(get_logger(), "Robot-base odom [x,y,yaw]=[%f %f %f]",
                 robot_pose_.translation()(0),
                 robot_pose_.translation()(1),
                 rf2o::getYaw(robot_pose_.rotation()));
@@ -985,18 +985,29 @@ void CLaserOdometry2D::PoseUpdate()
   //-------------------------------------------------------------------------------------
   double time_inc_sec = (current_scan_time - last_odom_time).seconds();
   last_odom_time = current_scan_time;
-  lin_speed = acu_trans(0,2) / time_inc_sec;
-  //double lin_speed = sqrt( mrpt::math::square(robot_oldpose.x()-robot_pose.x()) + mrpt::math::square(robot_oldpose.y()-robot_pose.y()) )/time_inc_sec;
-
-  double ang_inc = rf2o::getYaw(robot_pose_.rotation()) -
-      rf2o::getYaw(robot_oldpose_.rotation());
-
-  if (ang_inc > 3.14159)
-    ang_inc -= 2*3.14159;
-  if (ang_inc < -3.14159)
-    ang_inc += 2*3.14159;
-
-  ang_speed = ang_inc/time_inc_sec;
+  if (std::isfinite(time_inc_sec) && time_inc_sec > 1e-6)
+  {
+    // Twist is expressed in the child (robot-base) frame. Using the raw
+    // laser-frame x increment reverses the reported velocity when the scanner
+    // is mounted with a pi-yaw offset, as it is on LAKSA.
+    const Pose3d robot_increment = robot_oldpose_.inverse() * robot_pose_;
+    lin_speed = robot_increment.translation()(0) / time_inc_sec;
+
+    double ang_inc = rf2o::getYaw(robot_pose_.rotation()) -
+        rf2o::getYaw(robot_oldpose_.rotation());
+
+    if (ang_inc > 3.14159)
+      ang_inc -= 2*3.14159;
+    if (ang_inc < -3.14159)
+      ang_inc += 2*3.14159;
+
+    ang_speed = ang_inc/time_inc_sec;
+  }
+  else
+  {
+    lin_speed = 0.0;
+    ang_speed = 0.0;
+  }
   robot_oldpose_ = robot_pose_;
 
   //filter speeds
diff --git a/src/CLaserOdometry2DNode.cpp b/src/CLaserOdometry2DNode.cpp
index 2206777..1af6d3c 100644
--- a/src/CLaserOdometry2DNode.cpp
+++ b/src/CLaserOdometry2DNode.cpp
@@ -17,9 +17,17 @@
 
 #include "rf2o_laser_odometry/CLaserOdometry2DNode.hpp"
 
+#include <algorithm>
+#include <cmath>
+
 using namespace rf2o;
 
-CLaserOdometry2DNode::CLaserOdometry2DNode(): Node("CLaserOdometry2DNode")
+CLaserOdometry2DNode::CLaserOdometry2DNode():
+  Node("CLaserOdometry2DNode"),
+  new_scan_available(false),
+  accumulated_pose_variance{{0.0, 0.0, 0.0}},
+  previous_odom_time(0, 0, RCL_ROS_TIME),
+  have_previous_odom_time(false)
 {
   RCLCPP_INFO(get_logger(), "Initializing RF2O node...");
 
@@ -63,7 +71,7 @@ CLaserOdometry2DNode::CLaserOdometry2DNode(): Node("CLaserOdometry2DNode")
     initial_robot_pose.pose.pose.position.x = 0;
     initial_robot_pose.pose.pose.position.y = 0;
     initial_robot_pose.pose.pose.position.z = 0;
-    initial_robot_pose.pose.pose.orientation.w = 0;
+    initial_robot_pose.pose.pose.orientation.w = 1;
     initial_robot_pose.pose.pose.orientation.x = 0;
     initial_robot_pose.pose.pose.orientation.y = 0;
     initial_robot_pose.pose.pose.orientation.z = 0;
@@ -98,8 +106,13 @@ void CLaserOdometry2DNode::LaserCallBack(const sensor_msgs::msg::LaserScan::Shar
     else
     {
       // Initialize module on first scan (from laser params)
-      setLaserPoseFromTf();
+      if (!setLaserPoseFromTf())
+      {
+        return;
+      }
       rf2o_ref.init(last_scan, initial_robot_pose.pose.pose);
+      previous_odom_time = rf2o_ref.last_odom_time;
+      have_previous_odom_time = true;
       rf2o_ref.first_laser_scan = false;
     }
   }
@@ -122,8 +135,11 @@ bool CLaserOdometry2DNode::setLaserPoseFromTf()
   }
   catch (tf2::TransformException &ex)
   {
-    RCLCPP_ERROR(get_logger(), "%s",ex.what());
-    retrieved = false;
+    RCLCPP_WARN_THROTTLE(
+      get_logger(), *get_clock(), 2000,
+      "Waiting for TF from '%s' to '%s' before RF2O initialization: %s",
+      last_scan.header.frame_id.c_str(), base_frame_id.c_str(), ex.what());
+    return false;
   }
 
   // Keep this transform as Eigen Matrix3d
@@ -165,10 +181,17 @@ void CLaserOdometry2DNode::process()
   if( rf2o_ref.is_initialized() && scan_available() )
   {
     // Process odometry estimation
-    rf2o_ref.odometryCalculation(last_scan);
-
-    // Publish odometry over ROS2 (tf/topic)
-    publish();
+    if (rf2o_ref.odometryCalculation(last_scan))
+    {
+      // Publish odometry over ROS2 (tf/topic) only for a valid estimate.
+      publish();
+    }
+    else
+    {
+      RCLCPP_WARN_THROTTLE(
+        get_logger(), *get_clock(), 2000,
+        "RF2O rejected the latest scan pair; odometry was not published");
+    }
 
     // Do not run on the same data!
     new_scan_available = false;
@@ -176,7 +199,8 @@ void CLaserOdometry2DNode::process()
   else
   {
     // This is a warning. We depend on laser scans, so no meaning running faster than scan freq.
-    RCLCPP_WARN(get_logger(), "Waiting for laser_scans....");
+    RCLCPP_DEBUG_THROTTLE(
+      get_logger(), *get_clock(), 5000, "Waiting for laser scans");
   }
 }
 
@@ -223,6 +247,62 @@ void CLaserOdometry2DNode::publish()
   odom.twist.twist.linear.x = rf2o_ref.lin_speed;    //linear speed
   odom.twist.twist.linear.y = 0.0;
   odom.twist.twist.angular.z = rf2o_ref.ang_speed;   //angular speed
+
+  // RF2O exposes the covariance of its local planar scan increment in
+  // [x, y, yaw] order. Publish a conservative diagonal approximation instead
+  // of ROS' default all-zero covariance (which means perfect certainty).
+  const auto &increment_covariance = rf2o_ref.getIncrementCovariance();
+  const auto bounded_variance = [](double value, double lower, double upper) {
+    if (!std::isfinite(value) || value < 0.0)
+      return upper;
+    return std::max(lower, std::min(value, upper));
+  };
+  const double local_x_variance = bounded_variance(
+    increment_covariance(0, 0), 1e-6, 1e3);
+  const double local_y_variance = bounded_variance(
+    increment_covariance(1, 1), 1e-6, 1e3);
+  const double yaw_variance = bounded_variance(
+    increment_covariance(2, 2), 3e-6, 1e3);
+  const double yaw = rf2o::getYaw(rf2o_ref.robot_pose_.rotation());
+  const double cos_yaw = std::cos(yaw);
+  const double sin_yaw = std::sin(yaw);
+  const double global_x_variance =
+    cos_yaw * cos_yaw * local_x_variance +
+    sin_yaw * sin_yaw * local_y_variance;
+  const double global_y_variance =
+    sin_yaw * sin_yaw * local_x_variance +
+    cos_yaw * cos_yaw * local_y_variance;
+  accumulated_pose_variance[0] = std::min(
+    1e6, accumulated_pose_variance[0] + global_x_variance);
+  accumulated_pose_variance[1] = std::min(
+    1e6, accumulated_pose_variance[1] + global_y_variance);
+  accumulated_pose_variance[2] = std::min(
+    1e6, accumulated_pose_variance[2] + yaw_variance);
+
+  double scan_period = 1.0 / std::max(freq, 1.0);
+  if (have_previous_odom_time)
+  {
+    const double measured_period =
+      (rf2o_ref.last_odom_time - previous_odom_time).seconds();
+    if (std::isfinite(measured_period) && measured_period > 1e-4)
+      scan_period = measured_period;
+  }
+  previous_odom_time = rf2o_ref.last_odom_time;
+  have_previous_odom_time = true;
+  const double inverse_period_squared = 1.0 / (scan_period * scan_period);
+
+  odom.pose.covariance[0] = accumulated_pose_variance[0];
+  odom.pose.covariance[7] = accumulated_pose_variance[1];
+  odom.pose.covariance[14] = 1e6;
+  odom.pose.covariance[21] = 1e6;
+  odom.pose.covariance[28] = 1e6;
+  odom.pose.covariance[35] = accumulated_pose_variance[2];
+  odom.twist.covariance[0] = local_x_variance * inverse_period_squared;
+  odom.twist.covariance[7] = local_y_variance * inverse_period_squared;
+  odom.twist.covariance[14] = 1e6;
+  odom.twist.covariance[21] = 1e6;
+  odom.twist.covariance[28] = 1e6;
+  odom.twist.covariance[35] = yaw_variance * inverse_period_squared;
   //publish the message
   odom_pub->publish(odom);
 
@@ -265,4 +345,4 @@ int main(int argc, char** argv)
   }
 
   return 0;
-}
\ No newline at end of file
+}

/laksa_planning_lab/planner_server:
  ros__parameters:
    use_sim_time: false
    expected_planner_frequency: 1.0
    planner_plugins: [GridBased]
    GridBased:
      plugin: nav2_smac_planner/SmacPlannerHybrid
      motion_model_for_search: REEDS_SHEPP
      minimum_turning_radius: 1.09
      analytic_expansion_max_length: 5.0
      allow_unknown: false
      tolerance: 0.0
      downsample_costmap: false
      smooth_path: true
      # Values omitted by production are pinned to the ROS 2 Humble source defaults.
      downsampling_factor: 1
      angle_quantization_bins: 72
      max_iterations: 1000000
      max_on_approach_iterations: 1000
      cache_obstacle_heuristic: false
      reverse_penalty: 2.0
      change_penalty: 0.0
      non_straight_penalty: 1.2
      cost_penalty: 2.0
      retrospective_penalty: 0.015
      analytic_expansion_ratio: 3.5
      max_planning_time: 5.0
      lookup_table_size: 20.0
      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true
/laksa_planning_lab/global_costmap/global_costmap:
  ros__parameters:
    use_sim_time: false
    global_frame: map
    robot_base_frame: base_footprint
    update_frequency: 2.0
    publish_frequency: 1.0
    resolution: 0.05
    track_unknown_space: true
    footprint: "[[0.419, 0.148], [0.419, -0.148], [-0.149, -0.148], [-0.149, 0.148]]"
    plugins: [static_layer]
    static_layer:
      plugin: nav2_costmap_2d::StaticLayer
      map_topic: /laksa_planning_lab/map
      map_subscribe_transient_local: true
      subscribe_to_updates: false
    always_send_full_costmap: true
